syntax = "proto2";

message NestedObject {
  optional string id = 1;
  optional uint32 version = 2;

  /**
   * The use's email, downcased for the unique index.
   */
  optional string name = 4;
  optional string email = 5;
  optional string password = 6;
}
